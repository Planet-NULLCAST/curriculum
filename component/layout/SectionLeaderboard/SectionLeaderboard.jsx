import styles from './SectionLeaderboard.module.scss';

export default function SectionLeaderboard() {
    return (
        <section className={styles.section}>
            
        </section>
    );
}